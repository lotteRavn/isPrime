"use strict"

const isPrime = function (prime) {
	while (n = false) {
	    for (var i = 3; i < math.sqrt(arg); i += 2) {
	        if (prime % i === 0) {
	            return false;
	        	}
	        return true;
	    }

	}
let start = new Date();
let b = isPrime(prime);
let stop = new Date();
let elapsed = stop - start;
}